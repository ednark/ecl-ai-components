{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Registry Configuration",
  "description": "Declares how a component registry extends the base spec.",
  "type": "object",
  "required": [
    "name",
    "description",
    "agentMetaId",
    "tileDir",
    "facets",
    "indexSchemaVersion",
    "repo",
    "base"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Registry name (e.g., 'uswds-ai-components')"
    },
    "description": {
      "type": "string",
      "description": "One-line description of the registry"
    },
    "designSystem": {
      "type": "object",
      "description": "The design system this registry covers",
      "properties": {
        "name": { "type": "string" },
        "version": { "type": "string" },
        "url": { "type": "string" }
      }
    },
    "agentMetaId": {
      "type": "string",
      "description": "The HTML id of the embedded metadata block (e.g., 'uswds-agent-meta')"
    },
    "tileDir": {
      "type": "string",
      "description": "Directory containing component tiles (e.g., 'infinite')"
    },
    "facets": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of facet field names to aggregate in facets.json"
    },
    "indexSchemaVersion": {
      "type": "integer",
      "description": "Schema version for components.index.json"
    },
    "metadataSchemaVersion": {
      "type": "integer",
      "description": "Metadata schema version for tile agent-meta blocks. v1 = flat structure, v2 = categorized (discovery/selection/instruction/constraints)",
      "default": 1
    },
    "taxonomyVersion": {
      "type": "integer",
      "description": "Optional taxonomy version for the facet vocabulary"
    },
    "repo": {
      "type": "string",
      "description": "GitHub repo URL"
    },
    "base": {
      "type": "string",
      "description": "Base URL for raw file fetching (raw.githubusercontent.com/...)"
    },
    "additionalFields": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Non-standard fields included in each tile's metadata"
    },
    "govCompliance": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Government compliance standards this registry supports"
    },
    "supportedDesignSystems": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Design systems that have classMapping entries in this registry's tiles (e.g., ['material', 'bootstrap'])"
    },
    "adapterBase": {
      "type": "string",
      "description": "Base URL for adapter registry (cross-design system semantic mappings)"
    },
    "compatibilityPath": {
      "type": "string",
      "description": "Path to the family-level compatibility map, relative to the registry root (default: compatibility.json; monorepo registries may point at a shared cross/ directory)",
      "default": "compatibility.json"
    },
    "mcp": {
      "type": "object",
      "description": "MCP server capability advertisement",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether this registry ships an MCP server entry point",
          "default": true
        },
        "server": {
          "type": "string",
          "description": "Path to the MCP server entry point (relative to registry root)",
          "default": "_base/mcp/server.mjs"
        },
        "transport": {
          "type": "string",
          "enum": ["stdio"],
          "description": "Transport supported by the server",
          "default": "stdio"
        },
        "tools": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Tool names exposed by the server",
          "default": [
            "search_components",
            "get_component",
            "list_facets",
            "get_index",
            "get_adapter",
            "translate_component",
            "get_recipe",
            "query_compliance",
            "get_versions"
          ]
        }
      }
    },
    "gaps": {
      "type": "array",
      "description": "Declared component-concept gaps: design-system components or patterns this registry intentionally does not tile, with the nearest available alternative. Agents query this instead of inferring from absence.",
      "items": {
        "type": "object",
        "required": ["concept", "status", "reason"],
        "properties": {
          "concept": { "type": "string", "description": "Component concept name (e.g. combobox, card, mega-menu)" },
          "status": { "type": "string", "enum": ["not_part_of_design_system", "deferred"], "description": "not_part_of_design_system = doctrine/pattern, do not invent; deferred = candidate for a future tile" },
          "nearestAlternative": { "type": "string", "description": "Tiled family or recipe to use instead" },
          "reason": { "type": "string" }
        }
      }
    },
    "tokenProfiles": {
      "type": "object",
      "description": "Named token override sets components can declare support for (e.g. highContrast, darkMode, compactDensity). Keys are profile names; values have description + tokenOverrides.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "description": { "type": "string" },
          "tokenOverrides": {
            "type": "object",
            "description": "Token name → override value applied when this profile is active"
          }
        }
      }
    },
    "updated": {
      "type": "string",
      "description": "Version string for the registry"
    }
  }
}
