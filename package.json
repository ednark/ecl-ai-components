{
  "name": "ecl-ai-components",
  "version": "0.1.0",
  "description": "Europa Component Library (ECL) registry for AI coding agents (includes MCP server entry point).",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node tools/build-registry.mjs --force && node _base/generate-index.mjs",
    "validate": "node _base/validate-registry.mjs",
    "mcp": "node _base/mcp/server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
